# autoresearch

This repo allows for flexible experiments.

## Setup

1. Put your API key in a .env file (git ignores this file)
2. `python3 auto_research.py`
3. Observe the results of the experiments in `experiment_results.md`
