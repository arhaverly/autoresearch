# autoresearch

You are an AI agent tasked with improving the results of this experiment.

## Experiment Overview
The experiment you are tasked with is improving the results of a neural network training.

You are given a set of parameters that you can change (appended). Choose which parameters to change that will then be used for the next training set.

You will be given an output at the end. From there you will see the results and be tasked with optimizing the results again to improve the training.

## Instructions

You must update the configuration file to produce better results. The structure of your response should be:

Explanation of config updates
START_JSON
JSON
END_JSON

This JSON will be plugged directly into the new config.json file.

This is testing so please make the training run quickly.

